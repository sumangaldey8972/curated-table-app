# Base URL of the curated-table-be API (must include the /api prefix).
# Use your machine's LAN IP (not localhost) when testing on a physical device
# or the Android emulator, e.g. http://192.168.1.20:5001/api
EXPO_PUBLIC_API_URL=http://localhost:5001/api
